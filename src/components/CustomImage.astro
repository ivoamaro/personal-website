---
import { Image } from "astro:assets";
const { src, alt, width = "", height = "", inferSize = false } = Astro.props;
---

<Image
    inferSize={inferSize}
    src={src}
    alt={alt}
    width={width}
    height={height}
    class="fade-image"
    loading="lazy"
    decoding="async"
    onload="this.classList.add('is-loaded')"
/>

<style>
    .fade-image {
        opacity: 0;
        transition: opacity 0.6s ease-in-out;
    }

    .fade-image.is-loaded {
        opacity: 1;
    }
</style>
