---
export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<header class="grid header space-t-4 md:space-t-6">
  <div class="col-full" data-animate="children" data-animate-group="page">
    <h1 class="title">{title}</h1>
    <slot />
  </div>
</header>

<style>
  .header {
    .title {
      font-size: var(--step-4);
      font-weight: var(--font-regular);
      margin-bottom: var(--space-6);
    }
  }
</style>
